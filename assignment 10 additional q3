#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin>>n;
    vector<int>a(n),b(n);
    for(int &x:a) cin>>x;
    for(int &x:b) cin>>x;

    unordered_map<int,int>mp1,mp2;
    for(int x:a) mp1[x]++;
    for(int x:b) mp2[x]++;

    cout<<(mp1==mp2 ? "true" : "false");
}
