#include <bits/stdc++.h>
using namespace std;

struct Node{
    int data;
    Node* next;
    Node(int x){data=x; next=NULL;}
};
Node* head=NULL;

void insert(int x){
    Node* n=new Node(x);
    if(!head){head=n;head->next=n;return;}
    Node* t=head;
    while(t->next!=head) t=t->next;
    t->next=n;
    n->next=head;
}

int main(){
    int x;
    while(cin>>x) insert(x);
    if(!head) return 0;
    Node* t=head;
    do{
        cout<<t->data<<" ";
        t=t->next;
    }while(t!=head);
    cout<<head->data;
}
