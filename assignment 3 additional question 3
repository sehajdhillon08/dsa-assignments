#include <stdio.h>
#include <string.h>
#include <ctype.h>
int main() {
    char a[] = "listen", b[] = "silent";
    int count[26] = {0};
    for (int i = 0; a[i]; i++) count[tolower(a[i]) - 'a']++;
    for (int i = 0; b[i]; i++) count[tolower(b[i]) - 'a']--;
    for (int i = 0; i < 26; i++) if (count[i] != 0) {
        printf("Not Anagram\n");
        return 0;
    }
    printf("Anagram\n");
    return 0;
}
