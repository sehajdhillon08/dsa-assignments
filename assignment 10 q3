#include <bits/stdc++.h>
using namespace std;

int main(){
    int n; cin>>n;
    vector<int>a(n);
    for(int &x:a) cin>>x;

    unordered_map<int,int> mp;
    for(int x:a) mp[x]++;

    for(auto &p:mp) cout<<p.first<<" "<<p.second<<endl;
}
