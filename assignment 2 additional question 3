#include <stdio.h>
#include <string.h>

int main() {
    char s1[] = "listen";
    char s2[] = "silent";
    int count1[256] = {0}, count2[256] = {0}, i;
    for (i = 0; s1[i] && s2[i]; i++) {
        count1[s1[i]]++;
        count2[s2[i]]++;
    }
    if (strlen(s1) != strlen(s2)) {
        printf("Not Anagram");
        return 0;
    }
    for (i = 0; i < 256; i++) {
        if (count1[i] != count2[i]) {
            printf("Not Anagram");
            return 0;
        }
    }
    printf("Anagram");
    return 0;
}
