#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,m; cin>>n>>m;
    vector<int>A(n),B(m);
    for(int &x:A) cin>>x;
    for(int &x:B) cin>>x;

    unordered_set<int>s(A.begin(),A.end());
    for(int x:B) if(s.count(x)) cout<<x<<" ";
}
